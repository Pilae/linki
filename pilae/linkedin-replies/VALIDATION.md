# Validation record — 2026-09-21

An explicitly authorized, bounded read of the Quentin test conversation was performed. No message/invitation was sent, campaign launched or restarted, or production deployment performed. The operator reauthenticated the existing local Linki account during separate session debugging. At the time of the capture, the local Linki sandbox ran a login-only patch; PR #8 was installed later as described below. The application database was read-only for the conversation replay; synchronization tests used isolated databases.

## Implemented

Namespaced reader, durable account/conversation/message storage, pinned recipient identities, paginated synchronization, fenced leases and persistent provider ownership, independent scheduler, safe health/diagnostics, versioned reply events, campaign attribution and stop-on-reply hooks, authenticated manual API and `/pilae/linkedin-replies` status page. Shared upstream files changed: three. Premium implementation is absent; its public interface is preserved and its runner call is wrapped for ownership exclusion.

## Verified with synthetic evidence

- 29 reply fixtures pass (reader, synchronization, API/runtime boundary): duplicate delivery, conversation/message pages including backward history cursors and empty terminal pages, older outbound evidence arriving later, disk/database restart, partial error, an embedded inbound snapshot with unavailable history, atomic rollback, overlapping checks, stale leases, premium exclusion, mailbox identity change, pinned recipient identity, ambiguous recipients/campaigns, group/system/outbound/old messages, completed campaigns and account isolation.
- Contribution guard fixture passes: clean generic change accepted; Pilae paths and shared integration changes without markers rejected.
- Existing enrollment (11 tests), acceptance-sync and campaign-metrics fixtures pass. Existing Chromium fixtures pass: connection navigation, 34-language labels, localized counts, login signals and profile handling. Network disabled for those browser fixtures.
- Linki TypeScript check, scoped ESLint and production build pass. Build retains existing warnings about inferred workspace root and missing optional `@/ee`. Initial sandboxed build/browser attempts could not launch worker processes; reruns with process permission passed.
- Disposable built-server smoke test passes: unauthenticated request rejected, server credential accepted, synthetic signed-in UI renders never-checked status, manual button reports disabled provider, and no browser page errors. Only a fresh fixture DB and a disconnected synthetic account were used. The initial UI test used an overly exact label selector; the corrected combobox locator passes.
- Twenty companion: 34 adapter tests and 8 unit tests pass; TypeScript passes. The reply ingestion covers page checkpoints, duplicate delivery, deferred exact mappings, account mismatch rejection and partial feed failure. Status tests preserve manual decisions and email activity.
- Company-brain document validator: 0 errors, 22 existing warnings; unrelated human edits left intact. Companion patch reverse-application check passes against the edited integration snapshot.

## Authorized conversation read and correction

The bounded test observed an inbound reply after the campaign's recorded outbound action. An authenticated profile lookup verified the recipient's immutable profile URN. The conversation response differed from the original fixtures: participant `hostIdentityUrn` is at the top level, member links use internal profile identifiers, latest messages are embedded references, and metadata contains `newSyncToken` without historical-pagination proof. A session-context GET succeeded with outer quotes removed from the CSRF cookie value.

The corrected parser successfully replays the private, filtered capture: one conversation and one inbound message, with partial coverage. Synthetic regressions cover these structural details without committing names, real identifiers, bodies, session material or the capture. Separate checkpoints preserve sync tokens without treating them as backward-pagination cursors. Embedded messages survive partial checks, and incomplete coverage survives interrupted cycles without advancing `last_success`. Canonical profile resolution must return the exact participant URN before attribution.

The earlier GraphQL capture contains only the latest inbound message. Later, an authorized read of the exact thread in the operator's signed-in Chrome session showed the sender's outbound LinkedIn message at 19:34 and the recipient's inbound reply at 19:38, with distinct stable message IDs and exact profile identifiers. This corrected the earlier ambiguity about an email reply: the observed conversation is LinkedIn messaging. A read-only copy of the specific run, target, send log and tracks was replayed in an in-memory database with those two observed message IDs and timestamps. The synchronizer produced exactly one `linkedin_reply` outcome for the completed run and a `verified_reply` decision. No event was inserted into the application database. The replay supplied an artificial complete page and therefore does not establish a successful real inbox check or live provider pagination.

The local Linki login patch accepted a fresh operator login, but the saved session was rejected two minutes later with a 302/401 response. The authenticated Chrome read used a separate browser session; it does not validate Linki's server-side reader. The campaign remains completed and the existing application reply state is unchanged.

## Browser-page reader follow-up

PR #8 uses a serialized Linki account page, verifies that it reached the signed-in feed, and performs bounded same-origin reads from that page. This uses the normal Linki browser context rather than Playwright's separate request client. After the historical-page change, all 29 synthetic reply tests, TypeScript, scoped lint and a production build pass. The build warns that the optional `ee/` module is absent, as expected in this public checkout. A local-only image combines an earlier PR #8 build with PR #10's corrected login verification; it does not contain the new historical-page code, and `PILAE_REPLY_ACCOUNT_IDS` remains empty. Prior images are retained for rollback. The login route and reply page return 200, and the unauthenticated reply API returns 401.

After the operator completed a fresh login, a separate-process, Quentin-only diagnostic reached both the signed-in feed and exact thread with `li_at` and CSRF cookies intact. This verifies saved-session reuse for navigation in this attempt, not a complete inbox read. Repeated diagnostic runs observed one conversation-list GraphQL GET but no message-history request. Its count-only DOM probe found no elements matching its message-URN selector and no region meeting its scrollability threshold; those checks do not prove the page contained no messages. The first fixture had a sync-token root while the intercepted list request was category-based. A category-shaped fixture passed the installed parser's offline check, yet still produced no observable message query or scrollable region. The diagnostic blocked 30 non-GET requests, including one other Voyager endpoint; its effect on page bootstrap is unknown. The diagnostic was stopped rather than repeated further.

The operator then ran a different, read-only, exact-conversation diagnostic through Linki's saved session and Pilae's `SessionReader`, using a candidate message query ID from a public endpoint reference. It parsed one response page containing two unique ordinary messages: one from the sender, one from the other participant, and the captured latest inbound message ID. The response supplied a `newSyncToken` but no historical `nextCursor`, so the parser correctly reported partial coverage and the diagnostic reported `incomplete_history`, not a terminal history page. This validates a current query envelope and recent-message retrieval for this one conversation; it does not establish older-message pagination, a complete inbox check, or a persisted campaign outcome. No application reply event was inserted. No campaign was restarted, and no message or invitation was sent.

On 2026-09-22, with explicit authorization to run the tests, Codex ran a second Quentin-only read through the saved Linki session. The diagnostic opened the application SQLite database read-only, rejected any running campaign, resolved the recipient's internal profile link through `SessionReader.resolveProfile`, and required its immutable URN and canonical URL to match exactly one enrolled target. It read one recent-message page for the captured conversation and replayed the actual parsed messages with the recorded runs, enrollment and successful send log in an in-memory SQLite database. The inbound message ID and time matched the earlier authorized observation; the outbound message time was not exactly the send-log timestamp but met the detector's 60-second evidence rule. `projectReply` produced one `verified_reply` decision, one reply event and one `linkedin_reply` outcome in memory. The real response still had partial coverage and no historical cursor. A post-test read-only database check found zero application reply messages, zero application reply events and no running campaign. This validates exact-conversation attribution against one real recent snapshot, not the full synchronizer, persistent event delivery, Twenty projection, historical pagination or an account-wide successful check. No message or invitation was sent.

Public, AGPL-licensed [mautrix LinkedIn source](https://github.com/mautrix/linkedin/blob/v0.2608.0/pkg/linkedingo/messages.go) identified distinct timestamp-anchor and previous-cursor history queries. We used only protocol facts and built our own diagnostic. In a bounded Quentin-only live test through the saved Linki session, a one-message timestamp-anchor page contained the known inbound reply and exposed a `prevCursor`. Following it returned a separate one-message page containing the known outbound message, with an advanced `prevCursor`. One further cursor request returned a `messengerMessagesByConversation` root with an `elements` array instead of `*elements`; the strict diagnostic stopped before reading its cursor value. This verifies one backward pagination step and a changed shape at the older boundary. It does not prove a terminal cursor or complete history. No message body, identifier or cursor was logged, no application data was written, and no other conversation was read. The Pilae reader now independently supports these observed history requests and accepts `elements`/`*elements`, but requires an explicit `prevCursor: null` before claiming terminal history. Synthetic tests cover cursor advance, an empty terminal page and fail-closed unknown metadata. The new code has not been installed in the running local image.

## Not verified

One backward history step is verified for Quentin, but terminal cursors, longer histories and account-wide listing remain unverified; history query configuration is deliberately not enabled for polling. The mailbox identity endpoint through the full synchronizer, account-scale timing, persisted event delivery, and stop-on-reply behavior during actual outreach still need live validation. Exact internal-link profile resolution through the prior reader passed for Quentin only. Persisted sync tokens are evidence checkpoints only: incremental replay semantics are not yet implemented or claimed. A parsed latest-message snapshot is not a successful complete inbox check. The synchronizer stores embedded messages from the conversation list but does not project a campaign reply until a conversation-history page provides enough outbound evidence; it therefore does not treat the observed inbound snapshot as a verified live reply. Unsupported scopes and conservative attribution misses are described in the architecture guide. Premium internals are neither available nor tested.

The running local Linki service uses the combined PR #8 / PR #10 browser-global fix image with reply polling disabled. Twenty and the adapter continue using their previous loaded code. The Linki draft PR versions only the fork changes. The Twenty companion exists in the ignored integration folder and in its reproducible `patches/linkedin-replies.patch`, SHA-256 `77853a32ac6abb23da0e612a165f0c3b14870eceddaeffa30dc2148e75b3630e`. It is not a separate Git repository or included as implementation code in Linki's PR.
